extends layout.pug
include includes/command
include includes/icon
//- <!--suppress XmlUnboundNsPrefix, HtmlUnknownAttribute -->
block content
  header.header
    h1.header__title= title
  main.container
    form(action="/command" method="POST")#encrypt.tab__content.tab__content--selected
      .panel
        span.panel__label() TV Controller

        .panel__group
          +command('TV on', 'tv on')
            +icon('lock-unlocked')
          +command('TV off', 'tv off')
            +icon('lock-locked')

        .panel__group
          +command('Chromecast', 'tv chromecast').
            Chromecast
          +command('XBox', 'tv xbox').
            XBox
          +command('Raspberry', 'tv raspberry').
            Raspberry
